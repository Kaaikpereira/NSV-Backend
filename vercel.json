{
  "installCommand": "node scripts/write-gitconfig.js && GIT_CONFIG_GLOBAL=$PWD/.gitconfig npm install && cd node_modules/sna-456 && npm install && npm run build",
  "buildCommand": "npm run build",
  "functions": {
    "api/index.ts": {
      "includeFiles": "node_modules/sna-456/**"
    }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/api" }
  ]
}
