{
  "installCommand": "node scripts/write-gitconfig.js && GIT_CONFIG_GLOBAL=$PWD/.gitconfig npm install --ignore-scripts && cd node_modules/sna-456 && npm install --ignore-scripts && node node_modules/typescript/bin/tsc -p tsconfig.json",
  "buildCommand": "npm run build",
  "functions": {
    "api/index.ts": {
      "includeFiles": "node_modules/sna-456/**"
    }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/api" }
  ]
}
